[project]
name = "aprioribackend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.51.0",
    "qdrant-client",
    "openai",
    "pydantic",
    "python-dotenv",
]
