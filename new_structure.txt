config/
  pipelines/*.yaml          # declarative pipeline graphs/params
  schemas/*.json            # pydantic/jsonschema validation for configs
  evals/*.yaml              # evaluation suites (queries, assertions)

data/
  raw/
  processed/                # splits, keyword caches, expansions
  indexes/                  # exported index snapshots (optional)

src/app/
  core/
    types.py                # shared dataclasses (Query, Doc, Hit, Context, Answer)
    errors.py
  adapters/
    llm.py                  # chat/completions wrapper
    embeddings.py
    reranker.py             # LLM/cross-encoder interface
    vector_store.py         # Qdrant
    sparse_store.py         # (if using BM25/ColBERT/etc.)
  pipeline/
    executor.py             # orchestrates a pipeline graph
    registry.py             # loads/validates YAML → PipelineConfig
    models.py               # PipelineConfig, StepConfig, RuntimeContext
    hooks.py                # tracing/logging hooks (optional)
  steps/
    preprocess/
      clean_query.py
      expand_abbrev.py
      keywords.py           # offline keyword gen
      splitters.py
    ingest/
      embed_and_upsert.py
      rebuild_collection.py
    retrieval/
      dense.py
      sparse.py
      hybrid.py             # weighted fusion, multivector
      hyde.py
    rerank/
      llm_rerank.py
      cross_encoder.py
    augment/
      context_builder.py    # select top-k, windowing, dedup
      template.py           # prompt assembly
    postprocess/
      deduplicate.py
      threshold.py
      safety.py
    generate/
      answer.py             # wraps LLM call
  ui/
    app.py                  # Streamlit; calls pipeline.executor
    components/             # shared UI bits (spinners, results, debug panes)
  utils/
    timing.py, logging.py, batching.py
tests/
  unit/steps/
  integration/pipelines/
docs/
  diagrams/, guides/









name: multivector_weighted_v1
collection_name: baseline_hybrid
embedding:
  model: text-embedding-3-small
  dimensions: 1536
retrieval:
  top_k_vector: 15
llm_rerank:
  model: gpt-5-nano
  temperature: 0.2
  prompt: |
    You are a reranker. Given a query and a list of retrieved items,
    return the top items ordered by relevance. Consider semantic closeness
    and factual alignment.